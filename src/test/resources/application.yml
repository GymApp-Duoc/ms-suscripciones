spring:
  application:
    name: ms-suscripciones-test
  datasource:
    url: jdbc:h2:mem:testdb_suscripciones
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false

eureka:
  client:
    enabled: false

jwt:
  secret: testSecretKeyTotalmenteFalsaParaQueNoFalleElContextoDePruebas

server:
  port: 0